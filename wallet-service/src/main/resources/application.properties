spring.application.name=wallet-service



server.port=8084
spring.datasource.url=jdbc:mysql://localhost:3306/wallet_app?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=Usman@123

spring.jpa.show-sql=true

## Hibernate Properties
# The SQL dialect makes Hibernate generate beeter SQL for the chosen database
spring.jpa.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true

